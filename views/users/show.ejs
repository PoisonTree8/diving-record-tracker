
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= profile.username %>'s Dives</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <h1><%= profile.username %>'s Dives</h1>

  <% if (!dives || dives.length === 0) { %>
    <p>No dives yet.</p>
  <% } else { %>
    <ul>
      <% dives.forEach(d => { %>
        <li>
          <strong><%= d.location %></strong>  
          (<%= d.date ? new Date(d.date).toLocaleDateString() : '' %>)  
          — Type: <%= d.type %>, Depth: <%= d.depth %>,  
          Bottom Time: <%= d.bottomTime %>, Rating: <%= d.rating %>  
          <br />
          Notes: <%= d.notes && d.notes.trim() !== '' ? d.notes : '—' %>
        </li>
      <% }) %>
    </ul>
  <% } %>

  <p><a href="/users">← Back to Community</a></p>
</body>
</html>
